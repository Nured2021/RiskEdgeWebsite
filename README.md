# RiskEdgeWebsite

Simple personal site for RiskEdge Solutions (Nuredin Ibrahim).

## What I added ✅
- `index.html` — Main website content (About, Services, Portfolio, Training, Contact).
- `style.css` — Styling and responsive tweaks.
- `script.js` — Smooth scrolling behavior and button action.
- `images/placeholder.svg` — Small placeholder image used in the portfolio list.
- `docs/training-program.pdf` — A placeholder PDF file (replace with your real material later).

## How to preview locally 💡
1. Open `index.html` in any browser (double-click or `file://` path).
2. Or serve the folder with a simple HTTP server (recommended):

   Python 3: `python -m http.server 8000`

   Then open http://localhost:8000 in your browser.

## Next steps / Suggestions 🔧
- Replace `docs/training-program.pdf` with your real training PDF.
- Add real thumbnails to `images/` and update `index.html` portfolio items.
- If you want, I can add a deployment workflow (GitHub Pages) and a contact form.
